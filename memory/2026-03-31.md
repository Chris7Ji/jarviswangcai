# 记忆节点 [2026-03-31 22:48]

## 网站与AI新闻日报架构升级 (jarviswangcai.ai)
- **UI 更新**：全站导航栏中的“技能”按钮统一修改为“💡 技能与功能”。
- **功能升级 (多日视图)**：AI新闻日报页面 (`news.html`) 重构，不再只显示单日信息，现支持展示过去半个月（最多15天）的历史日报。
- **技术实现**：
  - 通过 `news_data/auto_news_v2.py`（及辅助脚本 `rerender_news.py`）读取 `news_db.json` 中的历史数据（按日期分组）。
  - 使用纯前端JS配合HTML模板生成多Tab/日期按钮界面 (`.date-pills`)。
  - 页面加载时默认激活并展示当天（最新）的日报内容，用户可通过点击历史日期按钮切换内容块。
  - 确保定时任务（22:00执行）在更新 `news.html` 时保留最新的导航结构和多日并排展示的完整 DOM 结构。